# TODO

## Low Priority

- [ ] Add `--no-tls` flag to CLI scripts and make `DTService` support insecure (plaintext) channels
- [ ] Add `--compression` flag to CLI scripts and pass `grpc.Compression.Gzip` through to the channel/stubs
